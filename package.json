{
  "name": "alphavector-re",
  "version": "0.1.0",
  "private": true,
  "description": "AV Dev Real Estate pack. Package alphavector-re. Bundle llc.alphavector.dev. Loads on alphavector-core. Not a consumer brand. Not the OS. Not Mission Control.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20" },
  "scripts": { "build": "tsc -p tsconfig.json", "test": "vitest run", "test:watch": "vitest", "lint": "tsc -p tsconfig.json --noEmit" },
  "license": "UNLICENSED",
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^2.1.8", "@types/node": "^20.17.10" }
}
